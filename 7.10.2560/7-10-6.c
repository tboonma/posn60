#include <stdio.h>
main()
{
    int x,y,i,j;
    scanf("%d %d",&x,&y);
    int a[x],b[y];
    for(i=0;i<=x;i++)
    {
        scanf("%d",a[i]);
    }
    for(j=0;j<=y;j++)
    {
        scanf("%d",a[j]);
    }

}
